<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent">
    <ImageView
        android:id="@+id/second_grid_item_iv"
        android:layout_alignTop="@+id/second_grid_item_tv"
        android:layout_alignBottom="@+id/second_grid_item_tv"
        android:layout_width="40dp"
        android:layout_height="wrap_content" />
<TextView
    android:id="@+id/second_grid_item_tv"
    android:layout_toRightOf="@+id/second_grid_item_iv"
    android:textSize="18sp"
    android:text="全部类目"
    android:textColor="@color/secondItemTextColor"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content" />
</RelativeLayout>